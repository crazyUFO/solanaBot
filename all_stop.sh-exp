#!/bin/bash

# 服务器列表
servers=(
   "user@server1"
    "user@server2"
    "user@server3"
)

# 循环通过 SSH 连接到每个服务器并执行命令来停止进程
for server in "${servers[@]}"; do
    echo "连接到 $server..."
    
    # 假设我们希望停止名为 solanaBot 的进程（根据实际进程修改）
    ssh -t "$server" 'pkill -f solanaBot'
done
